export default function EmptyFeed() {
    return (
        <div className="text-center py-20 text-gray-500">
            Your feed is empty. Start following communities or users to see posts!
        </div>
    );
}
